@inherits SemanticComponent
@namespace Sidea.SemanticUI.Blazor

<div class="@Class()">
    @ChildContent
</div>

@code {
    public Content() : base("content", false)
    {
    }

    [Parameter]
    public bool Extra { get; set; }

    [Parameter]
    public Position VerticalAligned { get; set; }

    [Parameter]
    public Position Floated { get; set; }

    protected override IEnumerable<string> Classes()
    {
        yield return this.VerticalAligned.ToVerticalAlignmentClass();
        yield return this.Floated.ToFloatedClass();
        yield return "extra".ToClassIf(this.Extra);
    }
}
